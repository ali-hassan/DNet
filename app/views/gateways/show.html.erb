<main class="main">
  <div class="main-content">
    <div class="payment-option-listing">
      <h2>Choose your Payment Option</h2>
      <h4 class="alert alert-info"> (Withdrawal Minimum Limit is 60$, Withdrawal Gates will open on every sunday from 10 am to 7 pm according to Hong Kong Time. One Account can apply one withdrawal in one Week with one Gate way only, Servies Fee is 10% .</h4>
      <div class="payment-option-holder">
        <form action="#">
          <ul>
            <li>
              <div class="image-holder">
                <%= link_to (current_user.withdraw_gateway.nil? || current_user.withdraw_gateway == "btc") && withdrawl_requests_url(subdomain: "office", withdraw_gateway: "btc") || "#" do %>
                    <%=image_tag "ic_bitcoin.png", alt: "image descriptions"%>
                <% end %>
              </div>
              <h3>Bitcoin</h3>
            </li>
            <li>
              <div class="image-holder">
                <%= link_to ( current_user.withdraw_gateway.nil? || current_user.withdraw_gateway == "skrill" ) && withdrawl_requests_url(subdomain: "office", withdraw_gateway: "skrill") || "#" do %>
                    <%=image_tag "ic_skrill.png", alt: "image descriptions"%>
                <% end %>
              </div>
              <h3>Skrill</h3>
            </li>
            <li>
              <div class="image-holder">
                <%= link_to (current_user.withdraw_gateway.nil? || current_user.withdraw_gateway == "perfect_money") && withdrawl_requests_url(subdomain: "office", withdraw_gateway: "perfect_money") || "#" do %>
                    <%=image_tag "ic_perfectmoney.png", alt: "image descriptions"%>
                <% end %>
              </div>
              <h3>Perfect Money</h3>
            </li>
            <li>
              <div class="image-holder">
                <%= link_to (current_user.withdraw_gateway.nil? || current_user.withdraw_gateway == "payoneer") && withdrawl_requests_url(subdomain: "office", withdraw_gateway: "payoneer")  || "#"  do %>
                    <%=image_tag "ic_payoneer.png", alt: "image descriptions"%>
                <% end %>
              </div>
              <h3>Payoneer</h3>
            </li>
          </ul>
        </form>
      </div>
    </div>
  </div>
</main>
